# Live credentials — never commit
.env
__pycache__/

trade_history.db
**/trade_history.db
trade_history.db-wal
trade_history.db-shm
trade_history.db-journal
*.db-wal
*.db-shm

# Runtime state, regenerated each session
rsi_state.json
*.log
logs/
backtest_cache/

# Virtualenv — was committed (8,067 files); git flattened its symlinks to
# text and dropped the exec bits, so it never had a working interpreter.
venv/
.venv/
requirements.lock.txt
